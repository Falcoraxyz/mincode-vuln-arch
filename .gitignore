# python
__pycache__/
*.pyc
.venv/
venv/

# editors / os
.DS_Store
Thumbs.db

# hermes internal
.ok/

# chain signing key (NEVER commit)
._chain/.key

# auto-generated MOC (regenerate with vault_index.py)
vault/Index.md

# build artifacts (wheel/sdist)
build/
dist/
*.egg-info/

# local vault default (created at runtime when no --vault / OBSIDIAN_VAULT_PATH)
mincode-vault/
