"""mincode-vuln-arch toolkit package.

Installed from the repo's scripts/ directory via pyproject.toml
(``package-dir = {"mincode_vuln_arch" = "scripts"}``). The ``mincode`` console
script entry point is ``mincode_vuln_arch.cli:main``.
"""
